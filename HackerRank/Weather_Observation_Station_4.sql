SELECT COUNT(*)-COUNT(DISTINCT CITY) FROM STATION;

-- DISTINCT CITY Returns the row of data with distinct city names, from the rest of the column.
-- COUNT(*) Counts the total no. of rows of data in a table
-- COUNT(Column_Name) can count the data in a specific tabl$e
