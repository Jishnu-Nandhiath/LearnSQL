SELECT name FROM Employee WHERE salary > 2000 AND months < 10 ORDER BY employee_id;

-- AND is used to combine two conditional statements. And the Query returns only if the row satisifies both the conditional statements.
